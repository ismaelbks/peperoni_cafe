<section class="container col-lg-12 text-center" id="reset">


	<h1>Réinitialisation du Mot de passe</h1>
	<br>
	<p>Entre ton email ici, nous t'enverrons un email contenant un lien pour le réinitialiser (valide pendant 2 heures)</p>

	<div class="row">
	  <div class="col-md-6 col-md-offset-3">
	    <%= form_for(:password_reset, url: password_resets_path) do |f| %>
	      <%= f.label :email %>
	      <%= f.email_field :email, class: 'form-control' %>
	      <br>

	      <%= f.submit "Envoyer l'email", class: "btn btn-primary" %>
	    <% end %>
	  </div>
	</div>

</section>